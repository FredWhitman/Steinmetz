✅ Great for:
Simplicity
Quick implementations
Teams familiar with procedural PHP


<-- inventoryActions.php -->
require_once 'inventoryHandlers.php';

$data = json_decode(file_get_contents("php://input"), true);
$action = $data['action'] ?? null;

$handlers = [
    'editProduct'  => 'handleEditProduct',
    'editMaterial' => 'handleEditMaterial',
    'editPFM'      => 'handleEditPFM'
];

if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($handlers[$action])) {
    call_user_func($handlers[$action], $data, $db, $util, $log);
} else {
    http_response_code(400);
    echo json_encode(['error' => 'Invalid action']);
}

<-- inventoryHandler.php -->
function handleEditProduct($data, $db, $util, $log) {
    // Do update logic for product
}

function handleEditMaterial($data, $db, $util, $log) {
    // Do update logic for material
}




